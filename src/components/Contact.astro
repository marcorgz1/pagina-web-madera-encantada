---
import { sendEmail } from '@/utils/email.ts'
import { Mail, MapPin, Phone } from '@lucide/astro'
import OnScrollReveal from 'astro-onscroll-animations'
import Map from './Map.astro'

let success = false
let errorMessage = ''

if (Astro.request.method === 'POST') {
    try {
        const data = await Astro.request.formData()
        const name = data.get('full-name')
        const email = data.get('email')
        const phone = data.get('phone-number')
        const message = data.get('message')

        if (!name || !email || !message) {
            throw new Error('No se han rellenado los campos obligatorios')
        }

        await sendEmail({
            name,
            email,
            phone,
            html: String(message),
        })

        success = true
    } catch (err) {
        errorMessage = err instanceof Error ? err.message : 'Error desconocido'
    }
}
---

<section
    id="contact"
    class="scroll-mt-48 scroll-animation scroll-animation-default"
>
    <div class="flex justify-center items-center mb-48">
        <div class="flex flex-col max-w-6xl gap-12">
            <div class="flex flex-col gap-6 mb-16">
                <h3
                    class="text-emerald-700 text-sm font-semibold tracking-[0.2em] uppercase"
                >
                    Contáctanos
                </h3>
                <h1
                    class="text-6xl text-gray-800/80 font-serif font-semibold"
                >
                    Vamos a planificar tu nueva casa
                    <br />
                </h1>
                <span class="text-xl text-amber-800/40">
                    ¿Tienes alguna duda? Nuestro equipo estará encantado de
                    ayudarte a escoger tu casa perfecta.
                </span>
            </div>
            <div class="flex justify-center items-center gap-2 mb-24">
                <ul class="flex justify-center items-center gap-16 w-full">
                    <li class="flex items-center w-sm px-8 py-6 border border-gray-200 shadow-sm rounded-2xl gap-8 hover:shadow-md">
                        <div class="w-12 h-12 bg-emerald-50 rounded-lg flex items-center justify-center flex-shrink-0">
                            <Mail class="w-6 h-6 text-emerald-800" />
                        </div>
                        <div class="flex flex-col gap-2">
                            <p class="text-gray-400 uppercase text-sm">Escríbenos</p>
                            <a
                                href="mailto:contact@madera-encantada.es"
                                >contact@madera-encantada.es</a
                            >
                        </div>

                    </li>
                    <li class="flex items-center gap-8 w-sm px-8 py-6 border border-gray-200 shadow-sm rounded-2xl hover:shadow-md">
                        <div class="w-12 h-12 bg-emerald-50 rounded-lg flex items-center justify-center flex-shrink-0">
                            <Phone class="w-6 h-6 text-emerald-800" />
                        </div>
                        <div class="flex flex-col gap-2">
                            <p class="text-gray-400 uppercase">Llámanos</p>
                            <div class="flex flex-col gap-2">
                                <a
                                    href="tel:+34652447192"
                                    class="hover:text-emerald-600 transition-colors"
                                >
                                    +34 652 447 192
                                </a>
                                <a
                                    href="tel:+34613336085"
                                    class="hover:text-emerald-600 transition-colors"
                                >
                                    +34 613 336 085
                                </a>
                            </div>
                        </div>
                    </li>
                    <li class="flex items-center px-8 py-6 border border-gray-200 shadow-sm w-sm rounded-2xl gap-8 hover:shadow-md">
                        <div class="w-12 h-12 bg-emerald-50 rounded-lg flex items-center justify-center flex-shrink-0">
                            <MapPin class="w-6 h-6 text-emerald-800" />
                        </div>
                        <div class="flex flex-col gap-2">
                            <p class="text-gray-400 uppercase">Dirección</p>
                            <p class="flex flex-col gap-12">
                                Tudela -Navarra
                                <br />
                                Polígono Las Labradas 1
                                <br />
                                Calle Estella nave C1
                            </p>
                        </div>
                    </li>
                </ul>
            </div>
        </div>
    </div>

    <OnScrollReveal />
</section>
